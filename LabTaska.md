You are designing a class Rectangle with two private member variables length and width.
The values for length and width should be passed at the time of object creation and
initialized through the constructor using an initialization list. How would you implement
the constructor?



Task 3
3. You have a base class(parent) Shape with a constructor(public shape(String color)) that initializes the color of the shape, and
a derived class Circle(Circle extends Shape) that also needs to initialize the radius. How would you design the
constructors in both classes to ensure that the color is properly set in the Shape class, and
the radius is set in the Circle class?

Task4
4. You are working on a class Person with attributes age and name. Implement a constructor
that ensures age is non-negative, and name is not empty. If any of these conditions are
violated, the constructor should throw an exception.